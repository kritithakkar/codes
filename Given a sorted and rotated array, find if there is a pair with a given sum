#include<stdio.h>
#include<stdlib.h>
int main(int argc,char *argv[])
{
    int n=atoi(argv[1]);
    int z=atoi(argv[2]);
    int arr[n];
    for(int i=3;i<argc;i++)
    {
        arr[i-3]=atoi(argv[i]);
    }
    int flag=0;
  for(int i=0;i<n;i++)
  {
      for(int j=i+1;j<=n;j++)
      {
          if(arr[i]+arr[j]==z)
          {
              printf("%d,%d\n",arr[i],arr[j]);
              flag=1;
          }
       
      }
  }
  if(flag==1)
  {
      printf("Yes");
  }
  else
  {
      printf("No");
  }
  
    return 0;
}
