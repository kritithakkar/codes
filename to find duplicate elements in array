#include <iostream>
#include<stdlib.h>
using namespace std;

int main(int argc,char *argv[])
{
int n=atoi(argv[1]);
int arr[n];
for(int i=2;i<argc;i++)
{
    arr[i-2]=atoi(argv[i]);
}

int flag=0;
int i;
for(i=0;i<n;i++)
{
    for(int j=i+1;j<n;j++)
    {
        if(arr[i]==arr[j])
        {
     cout<<arr[j]<<endl;
        }
    }
}
    return 0;
}
