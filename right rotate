#include<stdio.h>
#include<stdlib.h>
int main(int argc,char *argv[])
{
    int m=atoi(argv[1]);
    int n=atoi(argv[2]);
    int arr[n];
    for(int i=3;i<argc;i++)
    {
        arr[i-3]=atoi(argv[i]);
    }
    int j;
    for(int i=0;i<m;i++)
    {
        int temp=arr[n-1];
        for(j=n-1;j>0;j--)
        {
            arr[j]=arr[j-1];
        }
        arr[0]=temp;
    }
    for(int i=0;i<n;i++)
    {
        
        printf("%d ",arr[i]);
    }
    return 0;
}
