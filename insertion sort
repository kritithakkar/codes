#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,char *argv[])
{
 int n=atoi(argv[1]);
 int arr[n];
 for(int i=2;i<argc;i++)
 {
     arr[i-2]=atoi(argv[i]);
 }
 int temp,hole;
 for(int i=1;i<n;i++)
 {
     temp=arr[i];
     hole=i;
     while(hole>0 && arr[hole-1]>temp)
     {
         arr[hole]=arr[hole-1];
         hole=hole-1;
     }
     arr[hole]=temp;
 }
 for(int i=0;i<n;i++)
 {
     printf("%d ",arr[i]);
 }
    return 0;
}
